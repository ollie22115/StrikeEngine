#pragma once

#include <vector>
#include <initializer_list>

#include "Vertex.h"

namespace Strike {

	/*
	class VertexBuffer {
	public:
		VertexBuffer() = default;

		virtual void bind() = 0;
		virtual void unBind() = 0;

		virtual void setData(const uint32_t& size, const void* data, uint32_t usage) = 0;

		virtual void setVertexLayout(const VertexLayout& layout) = 0;

		virtual ~VertexBuffer();
	};

	class IndexBuffer {
	public:
		IndexBuffer() = default;

		virtual void bind() = 0;
		virtual void unBind() = 0;

		virtual void setData(const uint32_t& size, const void* data, uint32_t usage) = 0;

		virtual ~IndexBuffer();
	};
	*/
}